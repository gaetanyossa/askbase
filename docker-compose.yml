services:
  askbase:
    build: .
    ports:
      - "${PORT:-8080}:8080"
    env_file:
      - .env
    volumes:
      # Persist conversations & audit database across restarts
      - askbase-data:/app/data
    restart: unless-stopped
    environment:
      - PORT=8080

volumes:
  askbase-data:
